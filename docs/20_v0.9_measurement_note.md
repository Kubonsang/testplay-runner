# v0.9 사전 측정 — shadow warm-cache 임계값 가이드

**상태:** 측정 미실시. 본인 프로젝트(예: `~/Desktop/Dev/map`)에서 1회만 측정하면 됨.

**목적:** "에디터 attach 모드를 v1.x 로드맵에 올릴 것인가"를 데이터로 결정하기 위한 게이트.

---

## 측정 절차

`testplay run --shadow` 실행 시간을 다음 조건으로 측정한다 (모두 EditMode 기준).

### 단계 1 — Cold shadow

```bash
# 캐시 비우고 1회 측정
testplay run --shadow --clear-cache
# 출력의 manifest.json에서 started_at, finished_at 차이 확인
```

또는 `time` 사용:

```bash
time testplay run --shadow --clear-cache > /dev/null
```

기록할 값: **cold_seconds** (초 단위)

### 단계 2 — Warm shadow (5회 평균)

```bash
for i in 1 2 3 4 5; do
  time testplay run --shadow > /dev/null
done
```

5회 실행 시간을 평균. 첫 1회는 이미 단계 1에서 cold로 측정했으므로 별도로 5회 추가 실행.

기록할 값: **warm_avg_seconds**

### 단계 3 — 모델 한 턴 시간 측정

평소 작업 흐름의 "한 턴(에이전트 응답 한 사이클)" 평균 시간 측정. Claude Code 사용 중이라면 메시지 간 타임스탬프로 추정 가능. 외부 측정이 어려우면 다음 기본값 사용:

| 모델 | 기본 추정 (한 턴) |
|---|---|
| Opus 4.6 (thinking) | 60–120s |
| Sonnet 4.6 | 20–40s |
| Haiku 4.5 | 5–10s |

기록할 값: **turn_seconds**

---

## 결정 기준

비율 = `warm_avg_seconds / turn_seconds`

| 비율 | 의미 | 액션 |
|---|---|---|
| < 20% | shadow는 노이즈 안 | attach 모드 v1.x 로드맵 **불필요**. v0.9/v1.0 NGO에 집중. |
| 20–50% | 보임 | 정체성 락은 유지. 단, v1.x 로드맵 후보로 메모. shadow 자체 최적화(parallel copy 강화, ProjectSettings hardlink) 먼저 시도. |
| > 50% | 병목 | v1.x에서 **attach 마일스톤 신규 검토**. 단, 제안 단계에서 "에디터 상태 오염"·"Unity 버전별 TestRunnerApi 분기"·"두 백엔드 유지보수 비용" 세 가지 비용을 명시. |

**임계값을 보기 전에 attach 모드를 로드맵에 올리지 않는다 — CLAUDE.md의 Identity anchor 원칙.**

---

## 측정 결과 (1차: 2026-04-23)

| 항목 | 값 |
|---|---|
| 측정 일자 | 2026-04-23 |
| 측정 프로젝트 | `~/Desktop/Dev/map` (DungeonGen, 151 EditMode 테스트) |
| Unity 버전 | 6000.3.8f1 |
| testplay 버전 | v0.8.0 |
| `cold_seconds` | 223 (3:43) |
| `warm_seconds` (5회) | 30, 29, 33, 33, 27 |
| `warm_avg_seconds` | **30.4s** (편차 ±3s) |
| Cold→Warm 단축률 | 86% (v0.4.2 워밍 캐시 효과 검증) |

### 비율 분석 (모델별)

| 사용 모델 | 한 턴 | 비율 (`warm_avg / turn`) | 결정 게이트 |
|---|---|---|---|
| Opus 4.7 (thinking) | ~90s 추정 | **34%** | 20–50% — "보임. 정체성 락 유지, shadow 최적화 먼저" |
| Sonnet 4.6 | ~30s 추정 | **101%** | > 50% — "attach 마일스톤 검토 가치" |
| Haiku 4.5 | ~8s 추정 | **380%** | > 50% — "attach 검토" |

### 결정 (1차 측정 기준)

**현재 메인테이너 = Opus 4.7 사용 → 34% 구간 → attach 모드 v1.x 로드맵 미추가.**

근거:
1. 34%는 "보임" 구간이지 "병목" 구간이 아님
2. 정체성 락(contract layer ≠ speed layer)을 깰 정당성으로는 부족
3. Opus는 thinking 시간이 가변적 (60–120s) — 실제 비율은 25–50% 사이 변동, 평균은 33% 근처

**단, 다음 조건 중 하나라도 충족되면 재검토:**
- Sonnet/Haiku 사용자에게서 "Unity 대기 시간이 답답하다"는 외부 피드백 수신
- 측정 프로젝트 외 다른 프로젝트(예: 더 큰 프로젝트, Unity 2022 LTS) 측정에서 warm > 60s
- agent가 한 턴에 Unity 다회 실행 (예: 수정→재테스트→재수정) 패턴이 일반화되어 누적 대기 시간이 한 턴 시간을 명시적으로 초과

**다음 우선순위:**
1. v0.9 Network Primitives (계획대로 진행)
2. v1.0 NGO Harness (계획대로 진행)
3. (부수적) shadow 최적화 — `Assets/`/`ProjectSettings/` 더 공격적 parallel copy 등. v0.9/v1.0 사이 빈 시간이 있으면 검토. 마일스톤 아님.

**attach 모드는 명시적으로 out-of-scope:** RELEASE-PLAN.md의 "🚫 아직 약속하지 않는 것" 섹션에 이미 명시됨 (v0.9/v1.0 분리 커밋 `7375b07`).

---

## 참고

- v0.4.2의 Library 워밍 캐시가 이미 cold-start를 크게 줄였음. cold가 30초+ 라면 v0.4.2 회귀 가능성 의심.
- `--scenario` 모드는 cache write-back을 건너뛰므로(현재 의도된 동작) 측정 시 single-mode (`testplay run`) 쓸 것.
- 측정 결과가 어느 쪽이든, 결정의 근거를 이 문서에 남겨야 미래의 본인 또는 다른 메인테이너가 같은 논의를 반복하지 않음.
