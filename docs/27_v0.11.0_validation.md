# TestPlay Runner v0.11.0 — Validation Evidence

**Date:** 2026-07-14
**Hardening merge:** `c9fa4cdfbb1ee9681bf6c9598e846833d4c9c304` (PR #35)
**Primary Unity:** 6000.3.10f1, Windows
**Wire protocol:** 2

## Result

The v0.11 hardening gates passed on the primary Unity 6 / Windows target. The
bridge proved ownership for normal and sequential runs, refused foreign and
pre-existing-broken states, returned an indeterminate exit 9 without replay
across domain reload, sealed an old-session request after full Editor restart,
and recovered for later warm runs.

The user's GNF_ project then passed its release-candidate acceptance set through
the already-open Editor: 1/1, 3/3, and 49/49, plus an intentional no-match run
that returned exit 10 with zero tests.

## Automated gates

| Gate | Result |
|---|---|
| `go test ./... -count=1` | PASS |
| `go vet ./...` | PASS |
| CLI build | PASS |
| E2E-tag compile (`go test -tags=e2e ./e2e -run '^$'`) | PASS |
| Shell self-check | 9/9 PASS |
| Unity package EditMode tests | 11/11 PASS |
| GitHub Actions PR run 29332849095 | Ubuntu, macOS ARM, Windows PASS |
| Independent terminal-state reviews | No remaining P0/P1 |

The first PR CI run reproduced the base branch's Go 1.22/macOS 26 ARM
`missing LC_UUID` failure. The release branch moved macOS and release builds to
Go 1.26.4 while keeping Go 1.22.12 coverage on Ubuntu/Windows; the next matrix
run passed all three OS jobs.

## Unity protocol-2 spikes

| Spike | Expected | Observed |
|---|---|---|
| Normal warm run | Owned result, bridge backend | PASS |
| Three sequential warm runs | No stale `running_tests`; 3 bridge results | 3/3 PASS |
| Foreign long-running Test Runner run | Never captured as testplay result | Refused; foreign result not captured |
| Domain reload during owned run | One exit 9, no cold replay | run `20260714-211722-4118582c`, PASS |
| Run after domain reload | Editor recovers | run `20260714-211757-2a926674`, 1/1 PASS |
| Full Editor restart with old-session request | Tombstone, no response/XML | `20260714-212000-deadbeef`, PASS |
| Pre-existing broken compile | No stale-assembly false green | run `20260714-232016-7f98eeb7`, shadow exit 2 |
| Remove compile error and rerun | Same Editor returns to warm path | run `20260714-232121-57158b15`, bridge 1/1 PASS |

### Domain reload / indeterminate terminal

The run that forced a domain reload returned:

- backend: `bridge`
- exit: 9
- tests: 0
- warning: the run may have executed and was not rerun
- terminal events: exactly one `run_finished`, `phase: done`, `exit_code: 9`
- extra cold Unity process: none

The following marker-present run passed through the same warm Editor, proving
that the conservative result did not permanently wedge the bridge.

### Editor restart / stale-session request

An old request bound to session `20260714-114122-3effaa43` was left on disk, the
disposable Editor was fully stopped, and a new Editor session
`20260714-122104-14fa5e39` was started. The new server wrote:

```json
{
  "run_id": "20260714-212000-deadbeef",
  "execution_state": "possibly_started",
  "reason": "request belongs to bridge session '20260714-114122-3effaa43', current session is '20260714-122104-14fa5e39'"
}
```

No response file and no results XML were produced; the test was not executed.

### Pre-existing broken compile

The disposable Editor was first placed into a real `CS0103` failed-compilation
state. A filtered `PassingSmoke` request received a protocol-2 rejection:

```text
script compilation is failing in this editor (pre-existing broken state);
cold run will report the compile errors
```

The CLI selected `backend: "shadow"` and returned exit 2 with zero executed
tests. After the temporary error was removed, the same Editor immediately
returned `backend: "bridge"`, 1/1 pass. This proves both false-green prevention
and recovery.

## GNF_ release-candidate acceptance

The live project stayed on Unity PID 38980. No additional accessible Unity
process appeared during these four bridge runs.

| Filter | Run ID | Backend | Result |
|---|---|---|---|
| `RoomReviewHashTests.Sha256MatchesKnownVector` | `20260714-232522-e4793b70` | bridge | 1/1, exit 0 |
| `SpatialContractTests` | `20260714-232533-618d504f` | bridge | 3/3, exit 0 |
| `UnityDecoScene.DungeonDecorator` | `20260714-232544-8cd93b5b` | bridge | 49/49, exit 0 |
| `UnityDecoScene.DungeonDecorator.__V011_NO_SUCH_TEST__` | `20260714-232555-32974dae` | bridge | 0 tests, exit 10 + warning |

The candidate package was installed from an isolated immutable directory. The
project's `manifest.json`, `packages-lock.json`, and global v0.10 CLI were backed
up and hash-verified first. After acceptance, the two package files were restored
byte-for-byte to their original hashes and the Editor returned to protocol 1,
PID 38980, idle. The global CLI was not changed during candidate validation.

## Disclosures and limits

- The foreign-run bridge refusal was proven. Its disposable cold-shadow fallback
  later hit an unrelated Unity Curl cache database failure, so that particular
  spike did not claim a cold test result. Cold fallback completion was separately
  proven by the pre-existing-broken-compile spike, which reached authoritative
  shadow exit 2.
- v0.11 protocol-2 changes were not rerun on Unity 2022.3 LTS. v0.10 already
  proved the underlying package APIs and cold/warm parity on 2022.3.62f3; the
  official v0.11 revalidation target is Unity 6000.3.10f1 on Windows.
- The Release Preflight workflow must pass on the final merge commit before the
  annotated `v0.11.0` tag is pushed. Published tags are immutable; a release
  defect is fixed as v0.11.1, never by moving v0.11.0.
