# v0.13.0-rc.1 validation record

Status: **PREPARED — exact draft Windows asset validation pending**

## Candidate identity

- release-preparation base:
  `0bd5dde4da3b92be8a41fa9a98990aafe17b665b`;
- frozen provider checkpoint:
  `beabf36a299572607232806807ad9b9c2d4cb222`;
- branch: `codex/release-v0.13.0-rc.1`;
- intended tag: `v0.13.0-rc.1`;
- publication mode: draft prerelease followed by explicit approval;
- Windows signing: Authenticode **NOT SIGNED**;
- default backend promotion: **NOT PERFORMED**.

The release branch must not change provider logic after `beabf36`. Version,
documentation, security policy, workflow, and release-candidate harness changes
are the only allowed release-preparation differences.

## Reused native evidence

The native evidence summarized in
[`differencing-vhdx-workspace-provider.md`](differencing-vhdx-workspace-provider.md)
is reused because provider logic is frozen. Fixture/GNF 1/2/4 workers, forced
termination, broker restart, Windows reboot, quota/LRU, and retained workspace
were measured on native Windows with ownership-safe cleanup.

## Static evidence collected on the release branch

The local release-preparation gates were run on Windows 11 x64 on 2026-08-12.
Every PASS below is an observed process exit code 0, not an inferred result.
The GitHub Actions preflight run remains required after this branch is pushed
and before tag approval.

| Gate | Local result | Evidence |
|---|---|---|
| targeted storage/runsvc/config/CLI tests | PASS | `go test ./internal/vhdxworkspace ./internal/runsvc ./internal/config ./cmd/testplay ./internal/vhdxstorage ./internal/storagehelper -count=1` |
| full tests | PASS | `go test ./... -count=1`; a preceding run exposed one transient Windows share violation in an 8-worker ReFS test, the isolated retry passed, and the required full rerun then exited 0 |
| race detector | PASS | `go test -race ./... -count=1` |
| vet | PASS | `go vet ./...` |
| module integrity | PASS | `go mod verify` reported all modules verified |
| reachable vulnerability scan | PASS | `govulncheck v1.1.4 ./...` reported zero vulnerabilities reachable from this code; it separately reported unreachable findings in imported/required packages |
| Windows PowerShell 5.1 evidence/snapshot tests | PASS | all nine non-mutating `scripts/test-*.ps1` contracts exited 0; the administrator-only NVIDIA native gate is intentionally not part of this static set |
| workflow lint | PASS | `actionlint v1.7.7` on both release workflows |
| whitespace | PASS | `git diff --check` |
| GoReleaser configuration | PASS | GoReleaser v2.17.0 `check` |
| GoReleaser snapshot | PASS | snapshot version `0.13.0-rc.1-SNAPSHOT-0bd5dde`; 9 archives, 9 checksum entries, every archive hash verified |
| Windows snapshot contracts | PASS | extracted CLI version/help/storage/workspace inventory, accepted the valid `vhdx-diff` schema-1 config, rejected an invalid backend with exit 5, and passed storage-helper `hello` (`helperVersion=v2`, `provider=vhdx-differencing`, `requiresElevation=true`) |
| provider logic freeze | PASS | no changes under `internal/vhdxworkspace`, `internal/vhdxstorage`, `internal/storagehelper`, or `internal/runsvc`; the two Go changes between `beabf36` and the required base `0bd5dde` are CLI help strings only |

The local Go installation was 1.26.2. GoReleaser v2.17.0 selected Go 1.26.5
because that tool requires Go 1.26.4 or newer. Both release workflows pin Go
1.26.4 and must still pass in GitHub Actions.

## Remaining pre-tag gates

- push this release-preparation branch;
- run the GitHub Actions release preflight for `v0.13.0-rc.1` against
  `v0.12.0` and record its URL and conclusion;
- review the exact committed diff; private vulnerability reporting was enabled
  and verified through the GitHub REST API on 2026-08-12;
- obtain separate approval before creating the annotated tag.

## Exact draft asset gate

After explicit tag approval, the tag workflow must create a draft prerelease,
not a public release. Download the exact Windows CLI ZIP and `checksums.txt`,
verify the checksum and GitHub attestation, then run the guarded administrator
release-candidate harness with a unique store and artifact root.

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass `
  -File .\scripts\run-vhdx-diff-release-candidate.ps1 `
  -UnityEditorPath 'C:\Program Files\Unity\Hub\Editor\6000.3.8f1\Editor\Unity.exe' `
  -ReleaseArchivePath C:\path\to\testplay_0.13.0-rc.1_windows_amd64.zip `
  -ChecksumsPath C:\path\to\checksums.txt `
  -InstallApproved
```

The gate requires:

- fresh broker install and authenticated status;
- same-version broker upgrade;
- fixture EditMode 2/2 and PlayMode 1/1 with explicit `vhdx-diff` and no
  fallback;
- child release and ownership-safe uninstall;
- zero new service, receipt, store, workspace, VHDX, mount, file-backed disk,
  drive-letter, Unity, testplay, and broker residual.

Record the draft archive SHA-256, attestation verification, native artifact ZIP
and SHA-256 here. Until these fields are complete, the verdict remains:

```text
NOT YET VHDX_DIFF_EXPERIMENTAL_OPT_IN_RC_READY
```

## Not measured or claimed

- automatic `auto` default promotion;
- GNF_ 8-worker compatibility;
- long-running child growth;
- generalized performance superiority;
- production readiness;
- release readiness.
