# Dogfooding Session: gnf — 2026-07-10

> 한 줄 요약: v0.9.2 `run --shadow` 무필터 전체 스위트(486건) exit 0 green — 파이프라인 QA phase 회귀 게이트로 그대로 사용 가능, 1건 skip은 단일 프로세스 shadow 모드에서 NGO host+client 시나리오 테스트가 스킵된 것으로 보이며 실패가 아님.

- **Date:** 2026-07-10
- **Project:** gnf (Unity 6000.3.8f1, macOS/Darwin 25.5.0)
- **testplay version:** v0.9.2
- **Mode / backend:** single / shadow (출력에 `backend` 필드 없음 — 이 스키마 버전엔 미포함)
- **세션 목적:** PRESENT-01c-CURTAIN 태스크 파이프라인 QA phase의 전체 회귀 게이트 (필터 없음, task-pipeline.js가 요구하는 원장 증거)

---

## 실행한 명령

| # | 명령 | exit | 비고 |
|---|---|---|---|
| 1 | `testplay version` | 0 | schema_version 1, v0.9.2 |
| 2 | `testplay run --shadow` | 0 | 전체 스위트, 필터 없음, 소요 약 14분 (11:30:05→11:44:18) |

---

## 관찰 (발견 즉시 append)

### O1 — 무필터 전체 shadow 실행이 문서 계약대로 exit 0/486건 정확 집계 `severity: confirm`

- **재현:** `cd /Users/gubonsang/Desktop/Dev/GNF_ && testplay run --shadow`
- **관찰:** `exit_code: 0`, `passed: 485`, `failed: 0`, `skipped: 1`, `total: 486`, `errors: []`, `new_failures: null`
- **기대:** 필터 없이 실행하면 프로젝트의 전체 PlayMode 테스트가 하나도 누락되지 않고 집계되어야 함 (프로젝트 CLAUDE.md의 "전체 회귀" 관행이 이 계약에 의존)
- **실제:** 계약대로 동작. `total` 필드가 `passed+failed+skipped`와 정확히 일치(485+0+1=486).
- **우회:** 없음
- **비고:** 이전 세션(2026-06-17, project_full_regression_green.md)에서도 동일하게 exit 0 확인된 바 있어 회귀 없음.

### O2 — 시나리오 테스트 1건이 단일 프로세스 shadow 모드에서 Skipped `severity: low` `silent: no`

- **재현:** 동일 명령의 `tests[]` 배열에서 `ROOM_TRAVERSAL_Scenario_Tests.HostAndClient_ConnectAndObserveEachOther_InSameNgoSession` 항목이 `"result": "Skipped"`
- **관찰:** exit code나 `failed` 카운트에 영향 없음 — `skipped` 필드에 명시적으로 반영되어 결과가 조용히 사라지지 않음(non-silent)
- **기대:** 특별한 기대 없음 — 이름상 host+client 동시 연결을 요구하는 NGO 시나리오 테스트로 보여, 단일 인스턴스 shadow 모드에서 스킵되는 것이 프로젝트 테스트 설계상 의도된 동작으로 추정됨
- **실제:** `skipped: 1`로 정확히 카운트되어 리포트됨. testplay 쪽 문제라기보다 프로젝트 테스트 카테고리/Ignore 조건 쪽 동작으로 추정.
- **우회:** 없음 (조치 불필요 — 이 세션의 목적인 회귀 게이트에는 영향 없음)
- **비고:** 추정: 프로젝트의 scenario-mode(다중 인스턴스) 전용 테스트가 shadow 단일 모드에서 자체 skip 조건을 가진 것으로 보임. testplay 리포 측 결함 아님 — 정보성 기록.

---

## 잘 작동한 것

- 486건 무필터 전체 스위트가 약 14분 내 exit 0으로 완주, JSON 필드 정합성(`total = passed+failed+skipped`) 유지.
- `run_id`(`20260710-114220-0fc67333`)가 발급되어 이후 원장/대조 스크립트(record-evidence.sh 등)가 재현 가능한 anchor로 사용 가능.

## 원시 데이터 (발췌)

```json
{
  "exit_code": 0,
  "failed": 0,
  "new_failures": null,
  "passed": 485,
  "run_id": "20260710-114220-0fc67333",
  "schema_version": "1",
  "skipped": 1,
  "total": 486
}
```
